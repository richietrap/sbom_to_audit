# Stage 6.1 Baseline Hardening Report

## Status

`IMPLEMENTATION_CANDIDATE_AWAITING_MANUAL_EXECUTION`

## Preserved Stage 6 evidence

Stage 6 remains a deterministic computational ablation pilot. Its protocol, ADR-012, outputs,
and interpretation boundary are not overwritten.

## Remediated critical issues

| Stage 6 issue | Stage 6.1 control |
|---|---|
| Computational proxy substituted for manual baseline | Manual-assisted worksheet, release packets, timing, tools, and declarations |
| EC structurally favoured the schema | Locked EC retained beside common-field completeness |
| Confidence forced strict TR to zero | Analyst confidence permitted, required, and never imputed |
| EPG was tautological | Locked EPG retained beside equivalent record-bundle generation |
| CA conflated mechanism and human behaviour | Automatic safeguard ablation separated from recorded human clock awareness |
| Conflict ground truth depended on baseline output | Independent exhaustive event-level conflict oracle |
| Source counts used different units | Distinct artifact IDs used as the comparable measure |
| Protocol was not pre-execution frozen | Hash registry blocks drift before import and comparison |
| Mutable Colab reference | Stage 6.1 notebook rejects `main`, `master`, and placeholder refs |

## Remaining boundary

This package prepares and validates the experiment. It does not contain a genuine completed
manual baseline and therefore contains no final Stage 6.1 result. Manuscript eligibility remains
false until execution, import, immutable Git binding, and Colab reproduction are complete.
