# Stage 6.1 Paper Assets

This directory is intentionally empty before the manual baseline is completed. Candidate
assets are generated only by `scripts/build_stage6_1_paper_assets.py` after validation and
remain `CANDIDATE_NOT_FROZEN` until an immutable Git commit is reproduced in Colab.
