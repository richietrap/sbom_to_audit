# Stage 6.1 Pre-Execution Freeze

Run `python scripts/freeze_stage6_1_protocol.py` after all protocol, oracle, worksheet,
mapping, and scenario reviews are complete. The resulting JSON is committed before any
manual baseline execution. Hash drift blocks import and final comparison.
