# Environment records

Store one machine-readable environment record per frozen evaluation environment. Include operating system, Python version, processor, memory, package lock or freeze output, Colab runtime details where applicable, and the Git commit tested.
