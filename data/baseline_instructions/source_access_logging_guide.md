# Source Access Logging Guide

Create one row for every source access. Reopening a source at a later event requires another
row. The comparable headline count is the number of distinct artifact IDs accessed by each
workflow. Repeated accesses are reported separately and are not equated with machine source
ingestion or cognitive workload.
